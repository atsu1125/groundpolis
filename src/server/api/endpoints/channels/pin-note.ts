import define from '../../define';

export const meta = {
	requireCredential: true as const,

	desc: {
	},

	tags: ['channels'],

	params: {
	},
};

export default define(meta, async (ps) => {
	return;
});
